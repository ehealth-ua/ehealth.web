const fields = {
  message: {
    states: {
      default: { color: "shiningKnight" },
      errored: { color: "redPigment" },
      success: { color: "darkPastelGreen" }
    }
  }
};

export default fields;
