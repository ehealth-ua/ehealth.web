import React from "react";
import system from "system-components/emotion";

const Line = system({
  is: "figure",
  bg: "januaryDawn",
  my: 5,
  mx: 0,
  height: "1px",
  w: "100%"
});

export default Line;
