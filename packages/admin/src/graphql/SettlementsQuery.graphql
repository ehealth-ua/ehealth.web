query SettlementsQuery($name: String) {
  settlements(name: $name) {
    id
    district
    districtId
    koatuu
    mountainGroup
    name
    parentSettlement
    parentSettlementId
    region
    regionId
    type
  }
}
