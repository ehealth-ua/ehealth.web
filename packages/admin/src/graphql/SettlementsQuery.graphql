query SettlementsQuery($filter: SettlementFilter) {
  settlements(filter: $filter) {
    nodes {
      id
      district
      districtId
      koatuu
      mountainGroup
      name
      parentSettlement
      parentSettlementId
      region
      regionId
      type
    }
  }
}
