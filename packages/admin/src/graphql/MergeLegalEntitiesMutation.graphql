mutation MergeLegalEntitiesMutation($signedContent: SignedContent!) {
  mergeLegalEntities: mergeLegalEntities(
    input: { signedContent: $signedContent }
  ) {
    legalEntityMergeJob {
      id
    }
  }
}
