query MedicalProgramsQuery(
  $first: Int
  $filter: MedicalProgramFilter
  $orderBy: MedicalProgramOrderBy
  $skip: Boolean!
) {
  medicalPrograms(first: $first, filter: $filter, orderBy: $orderBy)
    @skip(if: $skip) {
    nodes {
      id
      databaseId
      name
      isActive
    }
  }
}
