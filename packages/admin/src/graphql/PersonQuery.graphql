query PersonQuery($id: ID!) {
  person(id: $id) {
    id
    status
    firstName
    secondName
    lastName
    birthDate
    birthCountry
    birthSettlement
    taxId
    unzr
    phones {
      type
      number
    }
    authenticationMethod {
      type
      phoneNumber
    }
  }
}
