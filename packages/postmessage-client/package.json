{
  "name": "@ehealth/postmessage-client",
  "version": "0.146.0",
  "private": true,
  "description": "",
  "main": "dist",
  "scripts": {
    "babel": "rm -fr dist && NODE_ENV=production babel src --out-dir dist",
    "start": "NODE_ENV=development babel src --out-dir dist --watch"
  }
}
